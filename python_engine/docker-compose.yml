services:
  python-engine:
    build: .
    environment:
      FLUXENTIQ_EMBEDDING_PROVIDER: ${FLUXENTIQ_EMBEDDING_PROVIDER:-disabled}
      SUPABASE_URL: ${SUPABASE_URL:-}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY:-}
    restart: unless-stopped
