import 'server-only'
export async function createEmbedding(text:string){if(!process.env.EMBEDDINGS_API_URL||!process.env.EMBEDDINGS_API_KEY)throw new Error('Embeddings service is not configured.');const r=await fetch(process.env.EMBEDDINGS_API_URL,{method:'POST',headers:{Authorization:`Bearer ${process.env.EMBEDDINGS_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({input:text}),cache:'no-store'});if(!r.ok)throw new Error(`Embeddings provider failed with ${r.status}.`);const d=await r.json();if(!Array.isArray(d.embedding))throw new Error('Embeddings provider returned no embedding.');return d.embedding as number[]}