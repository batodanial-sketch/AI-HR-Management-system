export const generateReport = async (type: string, filters: any) => {
  // Stub implementation
  return { reportId: crypto.randomUUID(), data: [] };
};

export const getReportTemplates = async () => {
  // Stub implementation
  return [];
};