import { describe, expect, it } from '@jest/globals'
describe('screening service contract', () => { it('does not report a fake assessment without a configured provider', () => expect(true).toBe(true)) })
