import{NextResponse,type NextRequest}from'next/server';export const runtime='nodejs';export async function GET(req:NextRequest){const provider=req.nextUrl.searchParams.get('provider');const returnTo=req.nextUrl.searchParams.get('returnTo')||'/';if(!provider)return NextResponse.redirect(new URL('/?auth_error=Missing+SSO+provider',req.url));return NextResponse.redirect(new URL(returnTo,req.url))}